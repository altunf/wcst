import styled from "styled-components";

export const Card = styled.div`
  margin-top: 3rem;
  margin-left: 3rem;
  height: var(--card-l);
  width: var(--card-l);
  background-color: white;
  border-radius: 8px;
  position: relative;
  display: flex;
`;
